<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shadui="clr-namespace:ShadUI">
    <ControlTheme x:Key="{x:Type shadui:CardDescription}" TargetType="{x:Type shadui:CardDescription}">
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource MutedColor}" />
        <Setter Property="TextBlock.FontSize" Value="12.8" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <ContentPresenter Content="{TemplateBinding Content}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </ControlTheme>
</ResourceDictionary>